interface:
  display_name: "Lumo Master Agent"
  short_description: "Run dynamic LumoAlpha subagent goal loops"
  default_prompt: "Use $lumo-master-agent to recover context, spawn isolated subagents for bounded goals, monitor progress, and review outcomes."
